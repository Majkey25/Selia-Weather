# Beta.21 verification

Version code 22 adds descriptive model spread to expanded hourly details. The median, provider probability, precipitation components, current conditions, daily totals and advice remain unchanged. There are no additional data requests.

- 343 Android unit tests passed. Debug/release lint and debug, release, signed AAB and release-optimized QA builds passed.
- Data regressions cover minority trace amounts, invalid and insufficient contributors, old caches, metadata length and numeric validation, count/range consistency and skipped-hour alignment.
- Huawei YAL-L21, Android 10: the separate minified QA app loaded real Prague data. For 9 September at 08:00, the central amount was 0 mm and provider probability 0%, while four of nine contributing models had a positive amount, with a range of 0–0.3 mm.
- English/Metric displayed the model count and range. After changing only the QA app to Czech/Imperial, the count remained four of nine and the displayed range became 0.00–0.01 in. The Czech decimal comma and diacritics rendered correctly.
- The dry summary now labels the provider probability instead of saying that measurable rain is not expected. The final wording was verified on the Czech/Imperial QA installation.
- The full-day sheet still showed 24 hours, scrolled through expanded statistics and dismissed by dragging its handle down. No AndroidRuntime errors were present for the final QA process.
- The QA clone and its temporary phone screenshot were removed before the next project's window. The production app, its settings and data, and the native viewport were not changed.

The count concerns numeric totals greater than zero, not every weather code indicating drizzle or an unquantified trace. It is not a probability. The model range is descriptive and is not a confidence interval or a validated accuracy gain. The reported September 7 drizzle event still lacks its exact location and original forecast inputs.
